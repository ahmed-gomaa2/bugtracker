import React from 'react';
import './Toolbar.scss';

const Toolbar = props => {
    return (
        <div className={'Toolbar'}>
            <div className="Toolbar__header">
                <h4>Home</h4>
            </div>
        </div>
    );
};

export default Toolbar;
